
## Run (VS Code)

This repo includes a VS Code launch configuration to run the full experiment (OpenVoice + Vevo) in one click.

1. Open the repo folder in VS Code.
2. Go to **Run and Debug**.
3. Select **Run Accent Conversion (OpenVoice+Vevo)**.
4. Press **Run**.

Outputs are written to `./output/`.

